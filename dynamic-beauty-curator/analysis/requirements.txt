# 리뷰 NLP 분석에 필요한 라이브러리
# 설치: pip install -r requirements.txt

pandas==2.2.3                 # CSV 읽기/처리

# 형태소 분석 (선택) — 기본 키워드 분석은 없어도 동작함.
# KoNLPy 설치는 Java(JDK)가 필요해서 까다로움.
# 우선 nlp_keywords.py의 기본 버전부터 돌려보고,
# 더 정교한 분석이 필요할 때 아래 주석을 풀어 설치.
# konlpy==0.6.0
